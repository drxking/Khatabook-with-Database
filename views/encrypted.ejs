<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Khaatabook - LogIn</title>
    <style>
        :root {
            --black: #000;
            --white: #fff;
        }

        @font-face {
            font-family: neue;
            src: url("/NeueMachina-Regular.otf");
        }

        * {
            font-family: neue;
        }

        input:focus,
        textarea:focus {
            outline: none;
        }

        input::placeholder,
        textarea::placeholder {
            color: rgba(128, 128, 128, 0.5);
        }
    </style>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <%if(message.length> 0){%>
        <div
            class="popup absolute left-1/2 top-2 text-sm text-center -translate-x-1/2 px-4 py-2 text-[--white] bg-red-500 rounded-md">
            <%=message%>
        </div>
        <%}%>
            <nav class="flex align-center justify-between px-14 py-8">
                <a href="/dashboard/<%=user._id%>">Khataabook</a>
                <ul class="flex gap-10">
                    <li><%=user.email%></li>
                </ul>
            </nav>
            <div class="section flex  items-center justify-center h-[75vh]">
                <form method="post" action="/encryptcheck/<%=hisaab._id%>" class="flex flex-col gap-3  max-w-80 items-center w-fit">
                    <h2 class="text-center"><b><%=hisaab.title%></b> is Encrypted. Please Enter Password to View</h2>
                    <input class="bg-gray-200 rounded-md px-6 py-2 w-full" type="password" name="password"
                        placeholder="Password">
                    <input class="bg-blue-500 rounded-md px-4 py-2 text-[--white]" type="submit" value="Submit">
                </form>
            </div>
</body>

</html>